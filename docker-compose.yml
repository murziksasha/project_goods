services:
  mongo:
    image: mongo:7
    container_name: project_goods_mongo
    restart: unless-stopped
    command: ["mongod", "--replSet", "rs0", "--bind_ip_all"]
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "try { rs.status().ok } catch (e) { rs.initiate({_id:'rs0',members:[{_id:0,host:'mongo:27017'}]}).ok }"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: project_goods_backend
    restart: unless-stopped
    depends_on:
      mongo:
        condition: service_healthy
    environment:
      PORT: 5000
      MONGO_URI: mongodb://mongo:27017/inventory?replicaSet=rs0
      CLIENT_ORIGIN: http://localhost:5173,http://127.0.0.1:5173,http://192.168.10.55:5173
      BACKUP_DIR: /app/backups
    volumes:
      - backup-data:/app/backups

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
      args:
        GIT_SHA: ${GIT_SHA:-unknown}
    container_name: project_goods_frontend
    restart: unless-stopped
    depends_on:
      - backend
    environment:
      API_PROXY_TARGET: http://backend:5000
    ports:
      - "5173:5173"

volumes:
  mongo-data:
  backup-data:
